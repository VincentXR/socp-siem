<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.socp</groupId>
        <artifactId>socp-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <artifactId>soc-base</artifactId>
    <name>SocBase</name>
    <description>SOC 基础（租户/用户/角色/审计消费者，SOCP2）</description>

    <dependencies>
        <dependency>
            <groupId>com.socp.platform</groupId>
            <artifactId>socp-client</artifactId>
        </dependency>
    <dependency>
            <groupId>com.socp.platform</groupId>
            <artifactId>socp-error</artifactId>
        </dependency>
        <dependency>
            <groupId>com.socp.platform</groupId>
            <artifactId>socp-tenant</artifactId>
        </dependency>
        <dependency>
            <groupId>com.socp.platform</groupId>
            <artifactId>socp-audit</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.socp.platform</groupId>
            <artifactId>socp-auth</artifactId>
        </dependency>
        <dependency>
            <groupId>com.socp.platform</groupId>
            <artifactId>socp-obs</artifactId>
        </dependency>
        <dependency>
            <groupId>com.socp.platform</groupId>
            <artifactId>socp-ratelimit</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- 审计落库：JPA + Flyway（PG 默认，H2 回退，见 application-h2.yml） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <!-- flyway-core 11.x 只内置 H2/SQLite 社区库，连 PostgreSQL 需单独模块 -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- 审计 Kafka 消费（socp-audit → t_audit，原生 client，与 detect-web 同款） -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
            <version>3.9.0</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
