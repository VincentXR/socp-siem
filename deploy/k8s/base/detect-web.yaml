apiVersion: apps/v1
kind: Deployment
metadata:
  name: detect-web
  namespace: socp-system
  labels: &detect-labels
    app.kubernetes.io/name: detect-web
    app.kubernetes.io/part-of: socp-siem
spec:
  replicas: 3
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels: *detect-labels
  template:
    metadata:
      labels: *detect-labels
    spec:
      serviceAccountName: socp-service
      terminationGracePeriodSeconds: 45
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: topology.kubernetes.io/zone
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: detect-web
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: detect-web
          image: ghcr.io/vincentxr/socp-detect-web@sha256:REPLACE_WITH_RELEASE_DIGEST
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: socp-runtime
            - secretRef:
                name: socp-runtime-secrets
          env:
            - name: SOCP_KAFKA_GROUP_ID
              value: socp-detect
            - name: SOCP_HEALTH_REQUIRED_ENDPOINTS
              value: kafka=kafka.socp-data.svc.cluster.local:9092,alert=alert-web:8080
          readinessProbe:
            httpGet:
              path: /detect-web/actuator/health/readiness
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /detect-web/actuator/health/liveness
              port: http
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /detect-web/actuator/health
              port: http
            periodSeconds: 5
            failureThreshold: 30
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1536Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
---
apiVersion: v1
kind: Service
metadata:
  name: detect-web
  namespace: socp-system
spec:
  selector:
    app.kubernetes.io/name: detect-web
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: detect-web
  namespace: socp-system
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: detect-web
